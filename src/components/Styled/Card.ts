import styled from 'styled-components'

import { FlexColCenterContainer } from './Flex'

export const Card = styled(FlexColCenterContainer)`
    border: 1px solid #000000;
    border-radius: 6px;
    padding: 12px;
`
